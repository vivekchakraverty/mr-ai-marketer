"""Email resolution + outreach: the free replacement for OpenOutreach's paid email lookup
(a deterministic pattern finder verified by self-hosted Reacher), plus the SMTP sender, the
IMAP reply reader, and the bridge to the app's Email Writer that drafts the actual copy."""
